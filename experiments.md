# Experiments

| Timestamp | Branch | Commit | Hypothesis | Outcome |
| --- | --- | --- | --- | --- |
| `20260319T220756` | `create-emulator` | `7511cabe362176ca5d5b5e42e51ac9bc73c14a8f` | A very small autoregressive CNN trained only on upper-layer thickness should be cheap to train, achieve a low one-step training loss, and provide a useful baseline for checking whether rollout evaluation, normalization, and artifact generation are wired correctly before trying larger architectures. | `eval_mse_mean = 28.4314`. The per-timestep eval MSE rises almost monotonically from `0.4361` at the first eval step to `66.9035` at the last, which suggests the baseline can track short horizons but accumulates autoregressive error steadily over the rollout. |
| `20260321T173440` | `2026-03-21-wider-deeper-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A slightly wider and deeper CNN with the same training setup should model spatial structure better and reduce autoregressive rollout error relative to the tiny baseline. | Negative result. `eval_mse_mean` worsened to `37.8978` from `28.4314`. The per-timestep eval MSE starts lower than the incumbent for the first several steps (`0.2292` vs `0.4361` at the first eval step) but steepens sharply later, reaching `165.7607` at the final step versus `66.9035` for the incumbent, so added capacity alone increased late-rollout drift instead of improving stability. |
| `20260321T174000-wider` | `2026-03-21-wider-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Increasing width alone from 16 to 32 channels while keeping depth fixed should improve spatial expressiveness without the extra optimization difficulty of a deeper network. | Strong negative result. `eval_mse_mean` exploded to `4738.6250`. The per-timestep eval MSE starts low (`0.2467`) but turns sharply upward after mid-rollout and blows up to `56232.6094` at the final step, indicating severe autoregressive instability. |
| `20260321T174100-deeper` | `2026-03-21-deeper-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Increasing depth alone from 3 to 5 layers should improve representational power enough to lower rollout error. | Mixed result. `eval_mse_mean` improved to `23.3452`, but the per-timestep error curve still steepens late and finishes worse than the incumbent at `96.9579` versus `66.9035`. This improves average error while degrading late-rollout stability, so it is not a clear new best. |
| `20260321T174200-kernel5` | `2026-03-21-kernel5-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A larger `5x5` kernel should capture broader local spatial structure and reduce rollout drift compared with the `3x3` baseline. | Negative result. `eval_mse_mean` worsened to `56.8036`, and the per-timestep eval MSE climbs faster throughout the rollout, ending at `248.6478`. Broader receptive field alone increased drift rather than flattening the error curve. |
| `20260321T174300-weight-decay` | `2026-03-21-weight-decay-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A small amount of L2 regularization should reduce overfitting to one-step pairs and improve autoregressive stability during rollout. | Positive result and new best emulator. `eval_mse_mean` improved to `16.8726` from `28.4314`, and the per-timestep eval MSE grows more slowly overall, finishing at `41.9651` instead of `66.9035`. The error series remains monotone, but its slope is flatter across the rollout, which is exactly the desired direction. |
| `20260321T174400-100-epochs` | `2026-03-21-100-epochs-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Doubling training to 100 epochs should reduce one-step error enough to improve rollout quality without changing architecture. | Negative result. Train loss dropped to `0.0012`, but `eval_mse_mean` worsened to `35.8856`, and the per-timestep eval MSE steepens into the late rollout, reaching `146.1904`. Lower one-step loss did not translate into better autoregressive behavior. |
| `20260321T174500-weight-decay-1e5` | `2026-03-21-weight-decay-1e5-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If the gain from L2 regularization is real but delicate, a lighter `1e-5` penalty may preserve the stability win while avoiding underfitting. | Negative result. `eval_mse_mean` worsened to `42.5567`, and the per-timestep eval MSE grows steadily to `127.4752` at the final step. The regularization was too weak to preserve the stability improvement. |
| `20260321T174600-weight-decay-5e4` | `2026-03-21-weight-decay-5e4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A moderately stronger L2 penalty than the current best may further flatten rollout drift by suppressing unstable one-step fitting. | Strong positive result and new best emulator. `eval_mse_mean` improved dramatically to `4.1520`, and the per-timestep eval MSE rises slowly from `0.2353` to only `9.9433` at the final step. Compared with the previous best (`16.8726` mean, `41.9651` final step), the error curve is much flatter across the full rollout. |
| `20260321T174700-weight-decay-1e3` | `2026-03-21-weight-decay-1e3-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If stronger L2 is helping, pushing to `1e-3` may regularize the rollout even more. | Positive but not best. `eval_mse_mean` improved to `6.4059`, and the per-timestep eval MSE ends at `15.3279`, better than the `1e-4` model but materially worse than the `5e-4` challenger. This suggests the useful regularization range extends above `1e-4`, but `1e-3` over-regularizes relative to the new optimum. |
| `20260321T174800-weight-decay-lr5e4` | `2026-03-21-weight-decay-lr5e4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If the regularized model is still optimizing too aggressively, lowering the learning rate to `5e-4` may improve rollout smoothness further. | Mixed but slightly worse than the old `1e-4` incumbent. `eval_mse_mean` came in at `13.0943`, but the per-timestep eval MSE still climbs to `43.1511` at the end of rollout, a little worse than the `1e-4`/default-lr run’s `41.9651` final step. Slower optimization did not beat the best regularization-only setting. |
| `20260321T174900-weight-decay-batch4` | `2026-03-21-weight-decay-batch4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If the regularized model benefits from lower gradient noise, increasing batch size from 2 to 4 may further reduce rollout drift. | Negative result. `eval_mse_mean` worsened to `21.3678`, and the per-timestep eval MSE reaches `76.4276` at the final step. The larger batch reduced optimization steps and produced worse late-rollout behavior. |
| `20260321T180000-weight-decay-2e4` | `2026-03-21-weight-decay-2e4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If `5e-4` is slightly too strong, reducing L2 to `2e-4` may retain stability while recovering some short-horizon fit. | Positive versus older baselines but not competitive with the incumbent. `eval_mse_mean` was `9.9691`, and the per-timestep eval MSE rises to `25.2192` at the final step. The curve is flatter than the `1e-4` model but materially worse than the `5e-4` best run. |
| `20260321T180100-weight-decay-3e4` | `2026-03-21-weight-decay-3e4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If the regularization sweet spot sits just below `5e-4`, then `3e-4` may outperform the current best. | Negative relative to the incumbent. `eval_mse_mean` worsened to `12.9390`, and the per-timestep eval MSE grows to `31.7976` by the last step. This is stable, but clearly less flat than the `5e-4` run. |
| `20260321T180200-weight-decay-7e4` | `2026-03-21-weight-decay-7e4-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If the optimum is slightly above `5e-4`, then `7e-4` may improve rollout smoothness further. | Close but not a new best. `eval_mse_mean` came in at `5.8035`, and the per-timestep eval MSE reaches `14.1501` at the final step. This is substantially better than most challengers, but still worse than the `5e-4` incumbent’s `4.1520` mean and `9.9433` final-step error. |
| `20260321T180300-weight-decay-2e3` | `2026-03-21-weight-decay-2e3-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If stronger regularization continues helping, pushing to `2e-3` may flatten the rollout error curve even more. | Negative result. `eval_mse_mean` worsened to `13.0333`, and the per-timestep eval MSE ends at `34.6952`. This confirms that regularization above the current range over-constrains the model. |
| `20260321T180400-weight-decay-5e4-100-epochs` | `2026-03-21-weight-decay-5e4-100-epochs-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If `5e-4` is the right regularization scale, training it longer may further improve rollout quality. | Negative result. Train loss dropped to `0.0014`, but `eval_mse_mean` worsened to `15.0191`, and the per-timestep eval MSE reaches `38.9614` at the final step. As in earlier runs, better one-step optimization did not improve autoregressive behavior. |
| `20260321T181000-residual-standard` | `2026-03-21-residual-standard-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A residual CNN with global skip connection should improve optimization and rollout stability relative to the plain CNN baseline at similar model size. | Mixed but not competitive with the incumbent. `eval_mse_mean` was `14.6797`, and the per-timestep eval MSE reaches `65.6497` at the final step. Early error is low, but drift accelerates too much later in rollout. |
| `20260321T181100-residual-deep` | `2026-03-21-residual-deep-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A deeper residual stack may exploit skip connections to gain representational power without the instability seen in deeper plain CNNs. | Strong negative result. `eval_mse_mean` exploded to `342.5043`, and the per-timestep eval MSE blows up from `0.2451` at the first eval step to `2142.7737` at the last. Extra depth was highly unstable even with residual connections. |
| `20260321T181200-residual-groupnorm` | `2026-03-21-residual-groupnorm-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Adding group normalization inside residual blocks may reduce internal drift and produce a flatter autoregressive error curve. | Positive architecture result but not a new best. `eval_mse_mean` improved to `6.1214`, and the per-timestep eval MSE ends at `24.4132`. This is much better than the standard residual stack, but still worse than the `5e-4` CNN incumbent. |
| `20260321T181300-residual-dilated` | `2026-03-21-residual-dilated-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Alternating dilated residual blocks may capture broader spatial structure and reduce rollout error growth. | Negative result. `eval_mse_mean` was `14.8075`, and the per-timestep eval MSE reaches `66.4802` at the final step. Dilated receptive field alone did not improve late-rollout behavior. |
| `20260321T181400-residual-depthwise` | `2026-03-21-residual-depthwise-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Depthwise-separable residual blocks may preserve the stability benefits of stronger regularization while learning spatial structure more efficiently than the plain CNN. | Positive result and new best emulator. `eval_mse_mean` improved to `3.9670` from the incumbent’s `4.1520`, and the per-timestep eval MSE stays flatter overall, ending at `12.3886`. This trades a slightly higher final-step error than the incumbent (`12.3886` vs `9.9433`) for a lower overall rollout mean, so it is a mixed but favorable architecture result. |
| `20260321T182000-residual-depthwise-groupnorm` | `2026-03-21-residual-depthwise-groupnorm-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Adding group normalization to the depthwise residual winner may reduce late-rollout drift without losing its low mean error. | Negative result. `eval_mse_mean` worsened to `12.4207`, and the per-timestep eval MSE reaches `51.9784` at the final step. Group normalization substantially hurt the depthwise architecture. |
| `20260321T182100-residual-depthwise-deep` | `2026-03-21-residual-depthwise-deep-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A deeper depthwise residual stack may improve long-horizon dynamics if the depthwise blocks remain stable at larger depth. | Strong negative result. `eval_mse_mean` exploded to `1771.1381`, and the per-timestep eval MSE blows up to `18195.9727` at the final step. Extra depth is decisively unstable in this family as well. |
| `20260321T182200-residual-depthwise-dilated` | `2026-03-21-residual-depthwise-dilated-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Adding dilation to the depthwise residual winner may improve spatial context while preserving its efficient inductive bias. | Slight positive result over the previous depthwise winner. `eval_mse_mean` improved marginally to `3.9338`, and the per-timestep eval MSE ends at `12.2752`. This is a very small gain on the mean metric with little improvement in late-rollout drift. |
| `20260321T182300-residual-depthwise-wide` | `2026-03-21-residual-depthwise-wide-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Widening the depthwise residual winner may improve capacity without the instability caused by extra depth. | Negative result. `eval_mse_mean` worsened to `6.4156`, and the per-timestep eval MSE rises to `23.1756` at the final step. More channels alone hurt rollout quality. |
| `20260321T182400-residual-depthwise-kernel5` | `2026-03-21-residual-depthwise-kernel5-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | A larger `5x5` kernel in the depthwise residual architecture may improve both average rollout accuracy and late-step stability by widening the local receptive field efficiently. | Positive result and new best emulator. `eval_mse_mean` improved to `2.6112`, and the per-timestep eval MSE remains much flatter overall, ending at `8.1768`. Unlike the previous depthwise winner, this model improves both the mean rollout metric and the final-step error, making it the clearest best result so far. |
| `20260321T183000-residual-depthwise-kernel5-dilated` | `2026-03-21-residual-depthwise-kernel5-dilated-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If `5x5` depthwise kernels are helping through broader spatial context, adding dilation on top may reduce rollout error further. | Negative result. `eval_mse_mean` worsened to `8.1274`, and the per-timestep eval MSE ends at `26.8794`. Once the kernel is already larger, added dilation hurts rather than helps. |
| `20260321T183100-residual-depthwise-kernel5-wide` | `2026-03-21-residual-depthwise-kernel5-wide-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Widening the `5x5` depthwise residual winner may increase useful capacity while preserving its improved receptive field. | Negative result. `eval_mse_mean` worsened to `8.3804`, and the per-timestep eval MSE reaches `26.3960` at the final step. The wider version is clearly worse than the incumbent. |
| `20260321T183200-residual-depthwise-kernel5-groupnorm` | `2026-03-21-residual-depthwise-kernel5-groupnorm-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Group normalization might stabilize the `5x5` depthwise residual winner further and improve late-rollout drift. | Negative result. `eval_mse_mean` worsened to `8.0853`, and the per-timestep eval MSE ends at `25.1285`. As in the smaller-kernel depthwise family, GroupNorm hurts rollout quality. |
| `20260321T183300-residual-depthwise-kernel5-deep` | `2026-03-21-residual-depthwise-kernel5-deep-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | With the stronger `5x5` spatial prior, adding depth may improve the model without the blow-up seen in smaller-kernel deep variants. | Mixed but not best. `eval_mse_mean` came in at `3.1880`, and the per-timestep eval MSE ends at `10.2741`. This is stable and much better than earlier deep variants, but still worse than the `4-block 5x5` incumbent on both mean and final-step error. |
| `20260321T183400-residual-depthwise-kernel7` | `2026-03-21-residual-depthwise-kernel7-cnn` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | If broader local context is the main source of improvement, pushing the kernel from `5x5` to `7x7` may help even more. | Negative result. `eval_mse_mean` worsened to `7.9017`, and the per-timestep eval MSE reaches `27.9766` at the final step. The benefit does not continue past `5x5`; `7x7` is too broad for this setup. |
| `20260322T000001-hpo-weight-decay-5e4` | `2026-03-22-cnn-weight-decay-tuning` | `812f31748388a7f04d9a36ad95c4f5c9c3d95426` | Hyperparameter-optimization mode. Tune the documented incumbent `cnn_thickness` baseline by changing only `weight_decay`, with fixed data source and evaluation. Exact tuned parameter: `weight_decay`, comparing incumbent `0.0` against tested value `5.0e-4`. Hypothesis: modest L2 regularization will materially improve rollout quality by reducing autoregressive drift. | Positive result and new best documented emulator on `main`. `eval_mse_mean` improved from `28.4314` to `4.1520`, and the per-timestep eval MSE is much flatter overall, ending at `9.9433` instead of `66.9035`. This looks like a meaningful tuning win rather than a marginal change, so the tuned challenger should replace the documented incumbent. |
