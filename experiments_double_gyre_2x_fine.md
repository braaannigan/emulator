# Experiments: `double_gyre_2x_fine`

| Timestamp | Branch | Commit | Hypothesis | Outcome |
| --- | --- | --- | --- | --- |
| `20260322T223500-dg2x-residual-depthwise-kernel5-longrun` | `main` | `38e6566` | Run the current best coarse-grid residual architecture, the `residual_thickness` depthwise-separable `5x5` model, on the explicitly requested fine-grid source run `20260322T200536`, which is long enough to provide a meaningful rollout evaluation. | Positive and much more informative than the earlier short-source runs. The model trained cleanly with `train_loss = 0.000289`, achieved `eval_mse_mean = 5.2373` over `49` eval steps, started at `0.00939` on the first eval step, and reached `15.8588` at the last. Both truth and rollout fields in `rollout.nc` are finite. This is the first usable long-horizon residual benchmark for `double_gyre_2x_fine`. |
| `20260322T222000-dg2x-cnn-weight-decay-5e4` | `main` | `38e6566` | With the repaired fine-grid source dataset, test whether the simpler coarse-grid CNN regularized with `weight_decay = 5e-4` transfers better than the current fine-grid residual incumbent in the very small-data regime. | Negative result. The CNN challenger completed cleanly but was worse than the current fine-grid residual incumbent on both available metrics: `eval_mse_mean = 1.3851` versus `1.1152`, and `train_loss = 0.01142` versus `0.00982`. Because the source dataset still yields only one eval step, this is weak evidence, but it does not justify replacing the residual incumbent. |
| `20260322T221000-dg2x-residual-depthwise-kernel5-dt300` | `main` | `38e6566` | After the initial fine-grid source run failed with NaNs, reduce the `double_gyre_2x_fine` generator timestep from `600s` to `300s`, regenerate the source data, and rerun the same coarse-grid incumbent emulator to test whether the instability was a generator-time-step issue rather than an emulator issue. | Positive generator fix and first usable fine-grid emulator result. The regenerated source run `20260322T220000-dt300` had `0` NaNs across all saved `layer_thickness` fields, and the emulator run completed with finite metrics: `eval_mse_mean = 1.1152`, `train_loss = 0.00982`, and a finite one-step rollout comparison. This is a usable first incumbent for `double_gyre_2x_fine`, but the evaluation is weak because the repaired source dataset contains only `10` saved timesteps, which yields just `7` training examples and `1` eval step. |
| `20260322T213000-dg2x-residual-depthwise-kernel5-firstpass` | `main` | `38e6566` | First-pass transfer experiment. Reuse the current best coarse-grid `double_gyre` emulator, the `residual_thickness` depthwise-separable `5x5` model, unchanged on the new `double_gyre_2x_fine` generator output to test whether the coarse incumbent provides a usable baseline at finer horizontal resolution. | Failed because the source generator dataset is not numerically usable. The emulator run completed, but `train_loss`, `eval_mse_mean`, and all per-step eval metrics were `NaN`. Inspection of source run `20260322T190617` showed that `layer_thickness` already contains `3,600,000` `NaN` values out of `5,760,000` total (`62.5%`), with NaNs appearing from timestep index `27` onward (`time_days = 189.0069`). This is not evidence for or against the emulator architecture; the first issue to fix is the fine-grid generator output. |
