# Experiments: `double_gyre_2x_fine`

| Timestamp | Branch | Commit | Hypothesis | Outcome |
| --- | --- | --- | --- | --- |
| `20260407T145901-dg2x-unet-mask-aware-padding` | `2026-04-07-dg2x-unet-mask-aware-padding` | `working tree` | Starting from the current best two-layer incumbent, keep the full `bilinear + add + stage_depth=2 + hidden_channels=24` recipe fixed and replace generic padded convolutions with mask-aware zero padding that renormalizes each receptive field using only in-domain cells, to test whether the remaining boundary artifact is caused by letting the U-Net treat outside-basin values as valid context. | Strong negative result. The mask-aware challenger completed in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260407T145901-dg2x-unet-mask-aware-padding/metrics.json) with `eval_mse_mean = 106.0579`, `eval_mse_max = 276.4442`, and `train_loss = 0.00644`, which is worse than the incumbent (`44.5149`), the reflect-padding challenger (`60.4601`), and the replicate-padding challenger (`72.7142`). A short `10`-iteration SkyDiscover follow-up from the same mask-aware base was started in [`topk_20260407_162023.log`](/Users/liambrannigan/playModels/emulator/outputs/topk/skydiscovery_0407_dg2x_maskaware_20ep_10iter/logs/topk_20260407_162023.log) and then stopped after the initial seed scored `106.1398`, iteration `1` regressed to `124.5729`, and the next candidate timed out at the one-hour evaluator limit. That is enough evidence to close the boundary-padding line for this `double_gyre_2x_fine` two-layer U-Net regime. |
| `20260407T130251-dg2x-unet-replicate-padding` | `2026-04-07-dg2x-unet-replicate-padding` | `working tree` | Starting from the current best two-layer incumbent, keep the full `bilinear + add + stage_depth=2 + hidden_channels=24` recipe fixed and replace zero convolution padding with `replicate` padding, to test whether preserving edge values without mirroring interior gradients reduces the remaining boundary ringing. | Negative manual follow-up. The replicate-padding challenger completed in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260407T130251-dg2x-unet-replicate-padding/metrics.json) with `eval_mse_mean = 72.7142`, `eval_mse_max = 169.0033`, and `train_loss = 0.00482`, which is worse than both the reflect-padding challenger (`60.4601`) and the incumbent `skyd-9b938741c1` (`44.5149`). Preserving copied edge values still leaves strong boundary artifacts, so simple padded-convolution variants are now low-value for this regime. |
| `20260406T202210-dg2x-skydiscovery-2layer-rerun-20ep-20iter` | `main+local` | `1e78676` | Relaunch the bounded SkyDiscover search on the correct `double_gyre_2x_fine` two-layer source `20260401T2LAYER-5000d`, keeping the same `20`-epoch / `20`-iteration budget, to test whether the earlier two-layer SkyDiscover gain is reproducible or whether the previous win depended on a narrow lucky draw. | Negative as a search follow-up and now concluded. The rerun completed all `20` iterations in [`topk_20260406_202210.log`](/Users/liambrannigan/playModels/emulator/outputs/topk/skydiscovery_0406_dg2x_2layer_rerun_20ep_20iter_pty/logs/topk_20260406_202210.log); its best candidate remained iteration `1`, program `31394b63-8c51-4fb2-8019-153133ef675e`, saved in [`best_program_info.json`](/Users/liambrannigan/playModels/emulator/outputs/topk/skydiscovery_0406_dg2x_2layer_rerun_20ep_20iter_pty/best/best_program_info.json). That model reached `eval_mse_mean = 73.9866`, `eval_mse_last = 174.0599`, and `train_loss = 0.00578` in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/skyd-a69573f021/metrics.json), improving on the rerun baseline `149.6612 / 323.0988` but failing to beat the earlier overall SkyDiscover leader at `44.5149 / 123.9231` from [`skyd-9b938741c1`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/skyd-9b938741c1/metrics.json). The rerun also wasted much of the second half of the budget on invalid proposals, especially repeated `stage_depth out of allowed range` failures, so more search in the current space looks low value. |
| `20260403T073925-dg2x-unet-reflect-padding` | `2026-04-03-dg2x-unet-reflect-padding` | `38f0f2b` | Starting from the current best two-layer incumbent, replace default zero convolution padding with `reflect` padding while holding the winning `bilinear + add + stage_depth=2 + hidden_channels=24` recipe fixed, to test whether the remaining edge artifacts come from boundary handling rather than the broader architecture. | Negative manual follow-up. The reflect-padding challenger finished cleanly but regressed the standard `250`-day rollout score to `eval_mse_mean = 60.4601` and `eval_mse_max = 164.8656` in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260403T073925/metrics.json), with `train_loss = 0.00475`. That is materially worse than the incumbent `skyd-9b938741c1`, which remains best at `44.5149 / 123.9231` in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/skyd-9b938741c1/metrics.json). The result suggests hard reflection injects the wrong boundary bias for this two-layer regime, so the next narrow manual follow-up should use `replicate` padding instead of reopening optimizer or architecture choices. |
| `20260402T163257-dg2x-unet-2layer-bilinear-upsampling` | `2026-04-02-dg2x-unet-bilinear-upsampling` | `e003e63` | The first two-layer incumbent captured the large-scale gyre structure but developed visible rollout hatching, especially in the velocity panels. Keep the entire two-layer U-Net regime fixed and change only `upsample_mode` from `transpose` to `bilinear` to test whether decoder aliasing is a material source of the artifact and whether that improves rollout error on the same source run `20260401T2LAYER-5000d`. | Positive on the standard `250`-day evaluation window and therefore the new incumbent for this experiment family. The bilinear challenger reduced `eval_mse_mean` from `100.8507` to `76.1132` and improved the late-rollout tail from `260.6110` to `214.4446`, while also lowering `train_loss` to `0.00493`, as recorded in [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T163257/metrics.json). The rollout animation [`comparison.mp4`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T163257/comparison.mp4) should now be treated as the primary qualitative reference for the two-layer line. A longer `500`-day probe still degraded strongly to `eval_mse_mean = 222.0762` and `eval_mse_last = 547.7827` in [`metrics_500d.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T163257/metrics_500d.json), so this change improves short-to-medium rollout quality but does not solve long-horizon stability in the turbulent two-layer regime. |
| `20260402T105528-dg2x-unet-2layer-multistep-dilated-residual` | `2026-04-01-dg2x-generator-2layer` | `1fdbafc` | Extend the current best shallow-water U-Net regime to the new turbulent `double_gyre_2x_fine` two-layer source run `20260401T2LAYER-5000d` by predicting all layer-thickness channels jointly while also conditioning on all-layer centered `u` and `v`. The goal is to establish a first two-layer incumbent and check whether the current multi-step dilated residual architecture remains qualitatively useful in the harder regime. | Positive as a first two-layer incumbent, but clearly much harder than the one-layer setting. The run trained cleanly and produced coherent rollouts with `h/u/v` structure over the `250`-day evaluation window, yielding `eval_mse_mean = 100.8507`, `eval_mse_min = 1.7474`, and `eval_mse_max = 260.6110` across `36` eval steps from [`metrics.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T105528/metrics.json). The promoted config is [`unet_thickness_double_gyre_2x_fine_2layer_window250_convnext_multistep2_dilated_residual.yaml`](/Users/liambrannigan/playModels/emulator/config/emulator/unet_thickness_double_gyre_2x_fine_2layer_window250_convnext_multistep2_dilated_residual.yaml), and the standard animation is [`comparison.mp4`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T105528/comparison.mp4). A longer `500`-day probe stayed finite but degraded substantially to `eval_mse_mean = 205.8690` and `eval_mse_last = 356.0559` in [`metrics_500d.json`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/20260402T105528/metrics_500d.json). Qualitatively, the emulator captures the large-scale gyre pattern but develops grid-scale hatching in the rollout velocity panels, which makes `upsample_mode=transpose` the next narrow artifact-targeting challenger. |
| `20260322T223500-dg2x-residual-depthwise-kernel5-longrun` | `main` | `38e6566` | Run the current best coarse-grid residual architecture, the `residual_thickness` depthwise-separable `5x5` model, on the explicitly requested fine-grid source run `20260322T200536`, which is long enough to provide a meaningful rollout evaluation. | Positive and much more informative than the earlier short-source runs. The model trained cleanly with `train_loss = 0.000289`, achieved `eval_mse_mean = 5.2373` over `49` eval steps, started at `0.00939` on the first eval step, and reached `15.8588` at the last. Both truth and rollout fields in `rollout.nc` are finite. This is the first usable long-horizon residual benchmark for `double_gyre_2x_fine`. |
| `20260322T222000-dg2x-cnn-weight-decay-5e4` | `main` | `38e6566` | With the repaired fine-grid source dataset, test whether the simpler coarse-grid CNN regularized with `weight_decay = 5e-4` transfers better than the current fine-grid residual incumbent in the very small-data regime. | Negative result. The CNN challenger completed cleanly but was worse than the current fine-grid residual incumbent on both available metrics: `eval_mse_mean = 1.3851` versus `1.1152`, and `train_loss = 0.01142` versus `0.00982`. Because the source dataset still yields only one eval step, this is weak evidence, but it does not justify replacing the residual incumbent. |
| `20260322T221000-dg2x-residual-depthwise-kernel5-dt300` | `main` | `38e6566` | After the initial fine-grid source run failed with NaNs, reduce the `double_gyre_2x_fine` generator timestep from `600s` to `300s`, regenerate the source data, and rerun the same coarse-grid incumbent emulator to test whether the instability was a generator-time-step issue rather than an emulator issue. | Positive generator fix and first usable fine-grid emulator result. The regenerated source run `20260322T220000-dt300` had `0` NaNs across all saved `layer_thickness` fields, and the emulator run completed with finite metrics: `eval_mse_mean = 1.1152`, `train_loss = 0.00982`, and a finite one-step rollout comparison. This is a usable first incumbent for `double_gyre_2x_fine`, but the evaluation is weak because the repaired source dataset contains only `10` saved timesteps, which yields just `7` training examples and `1` eval step. |
| `20260322T213000-dg2x-residual-depthwise-kernel5-firstpass` | `main` | `38e6566` | First-pass transfer experiment. Reuse the current best coarse-grid `double_gyre` emulator, the `residual_thickness` depthwise-separable `5x5` model, unchanged on the new `double_gyre_2x_fine` generator output to test whether the coarse incumbent provides a usable baseline at finer horizontal resolution. | Failed because the source generator dataset is not numerically usable. The emulator run completed, but `train_loss`, `eval_mse_mean`, and all per-step eval metrics were `NaN`. Inspection of source run `20260322T190617` showed that `layer_thickness` already contains `3,600,000` `NaN` values out of `5,760,000` total (`62.5%`), with NaNs appearing from timestep index `27` onward (`time_days = 189.0069`). This is not evidence for or against the emulator architecture; the first issue to fix is the fine-grid generator output. |

Next manual hypothesis: the boundary-padding line is now closed for the current `double_gyre_2x_fine` two-layer U-Net regime. The next manual follow-up should return to the actual incumbent [`skyd-9b938741c1`](/Users/liambrannigan/playModels/emulator/data/raw/double_gyre_2x_fine/emulator/unet_thickness/skyd-9b938741c1/metrics.json) and change something other than boundary handling, because `reflect`, `replicate`, `mask_aware`, and a short mask-aware SkyDiscover follow-up all failed to improve it.
